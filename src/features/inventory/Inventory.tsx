// Inventory wrapper - Tornadoes Job ERP
// Redirects to Stock component (legacy name)
import React from 'react';
import Stock from './Stock';

const Inventory: React.FC = () => {
  return <Stock />;
};

export default Inventory;
